crate::code_list_value_wrapper!(
    SurfaceMaterialValue,
    "The `surfaceMaterial` property of a TrafficArea/AuxiliaryTrafficArea. Shared across both per the CityGML 3.0 Transportation module."
);
