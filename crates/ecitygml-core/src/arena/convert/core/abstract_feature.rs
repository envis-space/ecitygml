use crate::arena::city_model_arena::CityModelArena;
use crate::model::core::AbstractFeature;

pub fn flatten_abstract_feature(
    _abstract_feature: &mut AbstractFeature,
    _city_model_arena: &mut CityModelArena,
) {
}
